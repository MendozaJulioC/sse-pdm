// aquí colocaré todas las rutas, tareas, consultas que tengan que ver con las fichas metodológicas delplan de desarrollo municipal

const { pool } = require('../sql/dbConfig');

const _postFichaCreate = async(req, res)=>{
  try {
      
    
  } catch (error) {
      console.log('Error _postFichaCreate: ', error)
  }  
}


module.exports={_postFichaCreate}